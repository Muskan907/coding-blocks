// it is a data structure and is the ordered collection of data
let nums = [1,4,67,43,56];
nums;
// nums.length; to print the kength of the array 
nums[1]; //to access any element we can use index

const randomArray = [1,2,3,"apple","boy"]; //heterogeneous

// array methods : push ,pop,shift,unshift,size,split,slice
// push() : add element at the end of the array
// pop() : remove element at the end of the array
// shift() : remove the first element of the array
// unshift() : add element at the beginning of the array
// splice() : remove or add element at the specified index
// slice() : 
// concat() : merge two arrays
// indexOf() : find the index of the element in the array
// join() :
// includes() :
// reverse() :

// const colors = ['red','orange','green','blue'];
// colors.slice(1,3)

// nested array
const nestedArray = [1,2,[3,4,5],6,7];
